# Linear-Regression-Model

Traditional C implementation with manual memory management and gradient descent algorithm
   - Interactive program that accepts training data points and performs linear regression
   - Uses gradient descent with configurable learning rate and epochs
   - Implemented in `main.c`